<section>
    <h3>{{post.title}}</h3>
    <h4>{{post.username}}</h4>
    <p>{{post.body}}</p>
</section>

<!--ignore

<div>
    {{#each comment as |comment|}}
    {{> comment}}
    {{/each}}
</div>

-->